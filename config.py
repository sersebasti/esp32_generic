# config.py
"""
Root-level config deprecated.

Use core/config.py. This module is intentionally non-functional
to avoid accidental reliance on a legacy shim.
"""

raise ImportError("config.py has been removed. Import from core.config instead.")
