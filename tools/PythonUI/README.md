# Per Tkinter non serve aggiungere a requirements.txt perché è incluso in Python standard, ma serve installarlo nel Dockerfile (già fatto)
# Per la GUI, il file principale sarà gui.py
# Esempio di avvio: python gui.py
